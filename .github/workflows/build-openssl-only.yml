name: Build Complete Full ARMv8 Optimized OpenSSL Only

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout OpenWrt Core Code
        uses: actions/checkout@v4
        with:
          repository: 'openwrt/openwrt'
          ref: 'openwrt-25.12'
          path: 'openwrt'

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget zstd libz3-dev

      - name: Set Up Package Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a

      # 🟩 THE ACCELERATION FORCER: Lock engine choices right inside OpenWrt's package profile definitions
      - name: Hardcode Hardware Crypto Defaults Into OpenSSL Rules
        run: |
          OPENSSL_CONFIG="openwrt/package/libs/openssl/Config.in"
          if [ -f "$OPENSSL_CONFIG" ]; then
            echo "⚙️ Hardcoding engine defaults into configuration rules..."
            sed -i 's|default n|default y|g' "$OPENSSL_CONFIG"
          fi

      - name: Configure M30 Target Architecture
        run: |
          cd openwrt
          echo "CONFIG_TARGET_mediatek=y" > .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic_DEVICE_dlink_aquila-pro-ai-m30-a1=y" >> .config
          
          # Target CPU speed enhancements
          echo 'CONFIG_TARGET_OPTIMIZATION="-O2 -pipe -march=armv8-a+crc+crypto -mtune=cortex-a53"' >> .config
          
          # Force hardware acceleration settings to stay on
          echo "CONFIG_PACKAGE_kmod-cryptodev=y" >> .config
          echo "CONFIG_OPENSSL_ENGINE=y" >> .config
          echo "CONFIG_OPENSSL_ENGINE_BUILTIN=y" >> .config
          echo "CONFIG_OPENSSL_ENGINE_BUILTIN_DEVCRYPTO=y" >> .config

          # Define targets
          echo "CONFIG_PACKAGE_libopenssl=y" >> .config
          echo "CONFIG_PACKAGE_libopenssl-conf=y" >> .config
          echo "CONFIG_PACKAGE_openssl-util=y" >> .config
          make defconfig

      - name: Run Unified Target Generation
        run: |
          cd openwrt
          export FORCE_UNSAFE_CONFIGURE=1
          make -j$(nproc)

      - name: Upload Artifact Packages
        uses: actions/upload-artifact@v4
        with:
          name: dlink-m30-openssl-only-packages
          path: openwrt/bin/
