# Version 1.0.1-SDK-Certified-HardwareCrypto-Pass
name: Fast Build Targeted SDK ARMv8 OpenSSL

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout OpenWrt Core Framework Structures
        uses: actions/checkout@v4
        with:
          repository: 'openwrt/openwrt'
          ref: 'openwrt-25.12'
          path: 'openwrt'

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget zstd libz3-dev

      - name: Inject True ARMv8 Hardware Assembly Flags into OpenSSL Settings
        run: |
          OPENSSL_MAKEFILE="openwrt/package/libs/openssl/Makefile"
          if [ -f "$OPENSSL_MAKEFILE" ]; then
            echo "⚙️ Injecting hardware architecture flag parameters into build tree..."
            
            # 1. Clear out size-optimizations (-Os) and activate your true hardware acceleration loops
            sed -i '/include \$(INCLUDE_DIR)\/package\.mk/i TARGET_CFLAGS:=\$(filter-out -Os, \$(TARGET_CFLAGS)) -O2 -pipe -march=armv8-a+crc+crypto -mtune=cortex-a53\nEXTRA_CFLAGS:=\$(filter-out -Os, \$(EXTRA_CFLAGS)) -O2 -pipe -march=armv8-a+crc+crypto -mtune=cortex-a53' "$OPENSSL_MAKEFILE"
            
            # 2. Hardcode the optimization engine configuration target selector straight to 64-bit ARM assembly
            sed -i 's|OPENSSL_TARGET:=.*|OPENSSL_TARGET:=linux-aarch64|g' "$OPENSSL_MAKEFILE"
          else
            echo "❌ Error: OpenSSL Makefile not found!" && exit 1
          fi

      - name: Seed Configuration Profiles for MediaTek M30 Block
        run: |
          cd openwrt
          echo "CONFIG_TARGET_mediatek=y" > .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic_DEVICE_dlink_aquila-pro-ai-m30-a1=y" >> .config
          
          # Target configuration options
          echo "CONFIG_PACKAGE_libopenssl=y" >> .config
          echo "CONFIG_PACKAGE_libopenssl-conf=y" >> .config
          echo "CONFIG_PACKAGE_openssl-util=y" >> .config
          echo 'CONFIG_TARGET_OPTIMIZATION="-O2 -pipe -march=armv8-a+crc+crypto -mtune=cortex-a53"' >> .config
          make defconfig

      - name: Run Rapid High-Speed Targeted OpenSSL Package Pass
        run: |
          cd openwrt
          export FORCE_UNSAFE_CONFIGURE=1
          
          # Force compile only your core crypto binaries inside the standalone profile
          make package/libs/openssl/compile V=s -j$(nproc)

      - name: Collect and Upload Your Fresh Accelerated OpenSSL APK Packages
        uses: actions/upload-artifact@v4
        with:
          name: dlink-m30-hardware-openssl-apks
          path: openwrt/bin/**/*.apk
